{
  "config": {
    "step": {
      "user": {
        "data": {
          "host": "Host",
          "password": "Password"
        },
        "data_description": {
          "host": "IP address of the modem's web interface. The factory default is 192.168.0.1.",
          "password": "The modem's admin password — the same one you use for its web UI or the tpMiFi app."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect / wrong password"
    },
    "abort": {
      "already_configured": "This modem is already configured"
    }
  }
}
